apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pennypilot
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - pennypilot.example.com
      secretName: pennypilot-tls
  rules:
    - host: pennypilot.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app
                port:
                  name: http
